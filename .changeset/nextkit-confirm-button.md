---
'@govcore/nextkit': minor
---

Add the `@govcore/nextkit/client` subpath with `ConfirmButton` — a submit button that runs a native confirm before letting its `<form>` submit. The one client wrinkle for destructive actions (delete) in otherwise-RSC screens; the RSC-only main entry is unchanged.
